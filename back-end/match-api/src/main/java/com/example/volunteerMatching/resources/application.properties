# Database Configuration
spring.datasource.url=jdbc:mysql://nabadatabase.cliacqmekc7s.us-east-2.rds.amazonaws.com:3306/nabaDatabase
spring.datasource.username=root
spring.datasource.password=nabaTest76!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update # or "none" if the schema is already created
spring.jpa.show-sql=true # Enable SQL logging for debugging

# Logging Level
logging.level.org.springframework=INFO

# Server Port
server.port=8080

# # H2 Database (In-memory)
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.datasource.driverClassName=org.h2.Driver
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
